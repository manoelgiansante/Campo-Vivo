{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "cd client && npx vite build --outDir ../dist --emptyOutDir && cd .. && ls -la dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
